# .secrets.yaml.example
# Copy to .secrets.yaml and fill in
# Only long-term credentials (API keys/secrets). Access tokens are cached at runtime in ~/.tradex/tokens/

data_provider:
  providers:
    zerodha:
      api_key: ""
      api_secret: ""
    dhan:
      api_key: ""
      api_secret: ""
      client_id: ""